name: Android Build

on:
  workflow_dispatch:



jobs:
  build:
      
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      
      - name: Setup JDK
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
          
      - name: Make gradlew executable
        run: chmod +x ./MobileFrontend/gradlew
          
      - name: Run Linter
        run: ./MobileFrontend/gradlew lint
          
      - name: Run Tests
        run: ./MobileFrontend/gradlew test
        
      - name: Build Project
        run: ./MobileFrontend/gradlew assemble
